# Project config

_templates_suffix: .cpr
_subdirectory: template
_answers_file: {{ project_name }}/.copier-answers.yml

# Questions
project_name:
  type: str
  help: Replace with your project name
  default: "my_project"

database:
  type: str
  help: Which database preset would you like to use?
  choices:
    Neon Postgres: neon-postgres
    Local Sqlite3: local-sqlite3

use_sentry_in_prod:
  type: bool
  default: false
  help: Do you want to use sentry's sdk?

use_celery:
  type: bool
  default: false
  help: Do you want to use celery?

use_redis:
  type: bool
  default: false
  help: Do you want to use redis?

# Messages to user

_message_before_copy: |
  Thanks for generating a project using our template.
  
  You'll be asked a series of questions whose answers will be used to
  generate a tailored project for you.

_message_after_copy: |
  Your project "{{ project_name }}" has been created successfully!
  
  Next steps:
  
  1. Change directory to the project root:
  
     $ cd {{ _copier_conf.dst_path }}
  
  2. Export DJANGO_SETTINGS_MODULE={{project_name}}.settings.dev to environmental variables
  
  3. 


_message_before_update: |
  Thanks for updating your project using our template.
  
  You'll be asked a series of questions whose answers are pre-populated
  with previously entered values. Feel free to change them as needed.

_message_after_update: |
  Your project "{{ project_name }}" has been updated successfully!
  In case there are any conflicts, please resolve them. Then,
  you're done.